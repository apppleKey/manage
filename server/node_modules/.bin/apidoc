#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_apidoc@0.20.0@apidoc/bin/apidoc" "$@"
  ret=$?
else 
  node  "$basedir/../_apidoc@0.20.0@apidoc/bin/apidoc" "$@"
  ret=$?
fi
exit $ret
